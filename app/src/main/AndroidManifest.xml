<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobility.iz.parallaxlistview" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ParallaxTestActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DashActivity"
            android:label="@string/app_name"
            android:theme="@style/TransparentABTheme" />
        <activity
            android:name=".ParallaxListViewActivity"
            android:label="@string/title_activity_parallax_list_view"
            >
        </activity>
    </application>

</manifest>
